# VSCode local settings
.vscode/settings.json
.vscode/*.log

# Lua local interpreter artifacts (used for local testing outside the game)
*.luac

# OS noise
Thumbs.db
.DS_Store

# Claude Code internal state (worktrees, session data)
.claude/

# Integration test live data — log files are large; keep them local only
test/live-data/

# Local review reports — kept out of the repo to avoid accidental commits
reviews/
# why: the weekly-review job writes .review/ and DAILY_REPORT.md into the
# working tree at every run; they showed as '??' at 1efc63e and one noisy
# report away from a `git add .`, so ignore them like reviews/.
.review/
DAILY_REPORT.md
.plan/
C:*
C*
